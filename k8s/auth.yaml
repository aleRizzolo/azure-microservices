apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-exam
  template:
    metadata:
      labels:
        app: auth-exam
    spec:
      containers:
        - name: auth-exam
          image: 
          volumeMounts:
            - name: 
              mountPath:
              readOnly: true
          ports:
            - containerPort: 3000
          imagePullPolicy: Always
      volumes:
        - name: 
          configMap:
            name: 
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth-exam
  ports:
    - port: 3000
      targetPort: 3000